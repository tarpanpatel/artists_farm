# Strict Local-Only & No-Deploy Rule

- **Do Not Deploy**: Never execute any deployment scripts, staging deployments (`deploy-staging.ps1`), production deployments, or remote server sync commands.
- **Local Environment Only**: All development, testing, builds, and verifications must take place strictly in the local environment (`c:\xampp\htdocs\artists_farm`).
