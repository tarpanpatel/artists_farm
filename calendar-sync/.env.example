DATABASE_URL=postgres://groundcode:change-me@localhost:5432/groundcode
REDIS_URL=redis://127.0.0.1:6379
CALENDAR_SYNC_PORT=4100
# The PHP/session validation endpoint called before a WebSocket joins a property.
CALENDAR_SYNC_SESSION_AUTH_URL=https://pms.example.com/php/api/calendar_session.php
# Per-channel webhook signing secrets. Never expose these to the browser.
AIRBNB_WEBHOOK_SECRET=change-me
BOOKING_COM_WEBHOOK_SECRET=change-me
